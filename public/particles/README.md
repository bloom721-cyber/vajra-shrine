# particles/

- `sprites/` — individual particle sprites with alpha (128 or 256 px)
- `configs/` — emitter definitions as `<name>.particle.json` (spawn rate, velocity, lifetime, blend mode)

Golden dust, incense smoke, falling petals. Keep motion slow and sparse — atmosphere, not spectacle.
